const mongoose = require('mongoose');


describe('The product_details database should contain 100 primary items afyer seeding', () => {
    beforeEach(() => {
        // TODO: provide DB connection
    })

    afterEach(() => {
        // TODO: clear database of seeded data
    })

    test('There should be 100 total item records after seeding the database', () => {
        // TODO: implment DB query to check for number of documents
    })
})